import React from 'react';
import { LoginForm } from '../../components/ui/LoginForm';

export const LoginPage: React.FC = () => {
    return (
        <>
            <LoginForm />
        </>
    );
};
